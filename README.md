### Currently use ElasticSearch as a demo
Need to configure OpenSearch with Auth


### Steps
Access localhost:5601 - kibana with username/password of ElasticSearch
Tab Discover -> Index -> Create index with `test*` and enjoy